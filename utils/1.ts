/*
|--------------------------------------------------------------------------
| 会員メール抽出
|--------------------------------------------------------------------------
*/
export function generateSqlQuery1(startDate: string, endDate: string): string {
  return `
    SELECT ...
  `;
}
